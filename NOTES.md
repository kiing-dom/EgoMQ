## Questions
- should jobs have unique IDs?
  - what other fields does a job need
- should handlers return a value?
- how should retries work?
  - independent of type?
- what happens if a worker crashes?
- should jobs be FIFO?
  - independent of type?
- how do delayed jobs fit into the queue?

