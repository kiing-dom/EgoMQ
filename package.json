{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "example": "bun examples/basic.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "vitest": "^4.1.10"
  }
}